# Editor / OS cruft
*~
*.swp
*.swo
.DS_Store
.idea/
.vscode/

# Local copies that may contain your real SSID / password / VPN details
*.local.sh
local/

# Runtime artifacts (in case anyone copies them into the repo)
*.leases
*.log

# per-user debloat change logs
logs/

# per-user root work dir (a fresh ~766MB OTA + extracted images per run).
# The vetted, device-specific images we DO ship live in onn-root/ (committed).
root-work/
ota*.zip
payload.bin
